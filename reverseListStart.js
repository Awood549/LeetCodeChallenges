var reverseList = function(head) {
    //instanciate a stack
    //fill the stack with the LL contents 
    //point the head to the tail and refill the LL
    let current = head;
    let temp;
    
    while(current.next){
        arr.push(current)
    }
    
};
